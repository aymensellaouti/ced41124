import { HighglightDirective } from './highglight.directive';

describe('HighglightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighglightDirective();
    expect(directive).toBeTruthy();
  });
});
